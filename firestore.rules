rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MODO DESARROLLO: Permitimos todo para diagnosticar si el problema es de reglas o de estructura
    match /{document=**} {
      allow read, write: if true;
    }
  }
}