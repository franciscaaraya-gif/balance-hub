rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla nuclear: permitir todo a cualquier usuario autenticado
    // Esto descarta problemas de permisos mientras depuramos el flujo
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}