rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // REGLA DE PROTOTIPO: Acceso total para desarrollo.
    // Permite lectura y escritura a cualquier usuario sin restricciones.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
